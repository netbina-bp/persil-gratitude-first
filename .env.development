# Development API base URL (no trailing slash).
# Point to your local PHP server, e.g. where api.php is served.
# Run: cd php && php -S localhost:8080 router.php
VITE_API_BASE_URL=http://localhost:8080
